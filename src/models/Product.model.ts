export interface ProductResponse {
  id: number;
  name: string;
  price: number;
  categoryId: number;
}

export interface Product {
  id: number;
  name: string;
  price: number;
  categoryId: number;
}
